# Network interceptor framework

Below commands will run the framework with an automated example of Metamask
Run:

```bash
npm install
./run-extensions.sh wallet-extensions-test.txt auto
```

If you want to work with manual mode:

```bash
./run-extensions.sh wallet-extensions-test.txt
```
