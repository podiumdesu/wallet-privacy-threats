// define a type for extension
export type ExtensionInfo = {
  id: string;
  name: string;
  pathID: string;
  extPath: string;
  baseProfileDir: string;
  tempProfileDir: string;
  password: string;
  seed: string;
};
